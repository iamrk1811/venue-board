# Local dev defaults — copy to .env and adjust as needed
# When running inside Docker these are overridden by docker-compose.yml
VITE_API_BASE_URL=http://localhost:8000
VITE_WS_BASE_URL=ws://localhost:8000
